#include <stdio.h>
#include <stdlib.h>

int main() {
  // int arr[] = {0, 23, 14, 12, 9};

  // int len = sizeof(arr)/sizeof(int);

  // int tmp;

  // for (int i=0; i<len;i++) {
  //   for (int j=i; j<len; j++) {
  //     if (arr[i] > arr[j]) {
  //       tmp = arr[i];
  //       arr[i] = arr[j];
  //       arr[j] = tmp;
  //     }
  //   }
  // }

  // for (int i=0; i<len; i++) {
  //   printf("%d ", arr[i]);
  // }

  // printf("\n");
  printf("%f\n", 3.1415);
  return 0;
}